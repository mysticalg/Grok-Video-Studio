PySide6==6.8.0.2
requests==2.32.3
google-api-python-client
google-auth-oauthlib
google-auth
playwright
websockets

# NOTE:
# - `google` was removed: it's a legacy meta-package and is not imported by the app.
# - `ffmpeg` (PyPI wrapper) was removed: the app shells out to system `ffmpeg` in PATH.
